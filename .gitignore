
__pycache__/

# Firebase's Python build creates venv/ in the source directory, which with
# source: "." is the repo root. .venv/ is the local development environment.
venv/
.venv/

outputs/

.DS_Store

# The corpus sheets are NDA-covered and never committed. Only the manifest is
# tracked; fixtures/sheets/ is populated by manual download —
# see tools/fetch_fixtures.py and docs/superpowers/specs/2026-08-06-regression-corpus-design.md
/fixtures/*
!/fixtures/MANIFEST.json

.regions_cache/
.scale_cache/
.room_labels_cache/

# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/

# W-gate census harness scratch state (pickled pages, ablation logs)
tools/census_scratch/cache/
tools/census_scratch/abl/
